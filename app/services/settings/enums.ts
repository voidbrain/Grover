export enum OperatingMode {
  On = 1,
  Off = 2,
  Silent = 3
}

export enum Purposes {
  client = 1,
  worker = 2
}

export enum Owner {
  schedule = 'schedule',
  user = 'user'
};

export enum Actions {
  read = 'read',
  execute = 'execute'
}

export enum Environments {
  room = 'room',
  location = 'location'
}
